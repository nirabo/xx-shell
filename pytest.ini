[pytest]
testpaths = tests
addopts = -v --cov=xx_shell --cov-report=term-missing --cov-report=html
python_files = test_*.py
markers =
    unit: unit tests
    integration: integration tests
    system: system tests
    performance: performance tests
    security: security tests
